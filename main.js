`use strict`;

const array = [];

const somma = Number(`50`);
let i = 0;


while () {

    let numb = Number(prompt(`Inserisci un numero`));
    while(isNaN(numb)){
        let numb = Number(prompt(`Inserisci un altro numero`));
    }

}
